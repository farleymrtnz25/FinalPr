{
  "name": "tu-proyecto",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "echo 'Building backend...'",
    "build:frontend": "mkdir -p dist && echo '<h1>Sitio en construcción</h1>' > dist/index.html"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "firebase-admin": "^13.4.0",
    "serverless-http": "^3.2.0"
  }
}
